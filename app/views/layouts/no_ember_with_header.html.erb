<!DOCTYPE html>
<html lang="<%= SiteSetting.default_locale %>">
  <head>
    <meta charset="utf-8">
    <title><%=SiteSetting.title%></title>
    <meta name="description" content="">
    <%= render partial: "layouts/head" %>
    <%= render partial: "common/special_font_face" %>
    <%= render partial: "common/discourse_stylesheet" %>
    <%= discourse_csrf_tags %>

    <%- unless customization_disabled? %>
        <%= raw SiteCustomization.custom_head_tag(session[:preview_style]) %>
    <%- end %>
    <%= yield(:no_ember_head) %>
  </head>
  <body>
    <%- unless customization_disabled? %>
      <%= SiteCustomization.custom_header(session[:preview_style], mobile_view? ? :mobile : :desktop) %>
    <%- end %>
    <section id="main">
      <header class="d-header clearfix">
        <div class="top-bar bg-color--black-dark">
          <div class="container">
            <div class="grid grid--middle grid--full">
              <div class="grid__item col-1-8">
                <a href="https://srcclr.com" class="color--white">
                  <i class="sci sci__src-logo--primary lh--" style="font-size: 5em;"></i>
                </a>
              </div>
              <div class="grid__item col-5-8">
                <ul class="nav nav--tabs inline-block bo--0 p0">
                  <li><a href="/" class="navbar__item navbar__link">Home</a></li>
                  <li><a href="/guides" class="navbar__item navbar__link">Guides</a></li>
                  <li><a href="/latest" class="navbar__item navbar__link">Discussions</a></li>
                  <li><a href="/projects" class="navbar__item navbar__link">Projects</a></li>
                  <li><a href="/nwslttr" class="navbar__item navbar__link">NWS:LTTR</a></li>
                </ul>
              </div>
            </div>
          </div>
        </div>
      </header>
      <div id="main-outlet" class="<%= @container_class ? @container_class : "wrap" %>">
        <%= yield %>
      </div>
    </section>
    <%- unless customization_disabled? %>
      <%= SiteCustomization.custom_footer(session[:preview_style], mobile_view? ? :mobile : :desktop) %>
    <%- end %>
  </body>
</html>
