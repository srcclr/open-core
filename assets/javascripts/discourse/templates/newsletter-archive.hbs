<h3 class="mb-">NWS:LTTR Archive</h3>

{{#conditional-loading-spinner condition=loading}}
  <table class="table--striped">
    <tbody>
      {{#each newsletter in model.newsletters}}
        <tr>
          <td>{{newsletter.title}}</td>
          <td><a href="/newsletters/{{newsletter.id}}/download" class="color--primary attachment" target="_">Download PDF</a></td>
          <td><a href="/newsletters/{{newsletter.id}}" class="color--primary" target="_">View</a></td>
        </tr>
      {{/each}}
    </tbody>
  </table>
  <div>
    <a href {{action "previousPage"}} {{bind-attr class="firstPage:hidden"}}>prev</a>
    <a href {{action "nextPage"}} {{bind-attr class=":pull--right lastPage:hidden"}}>next</a>
  </div>
{{/conditional-loading-spinner}}
